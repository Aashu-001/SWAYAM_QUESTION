class Solution {
public:
    int numTrees(int n) {
      if(n == 0 || n== 1){
        return 1;
      }
       int ans  =0 ;
      for(int  i= 1 ;i <=n;i++){
        int x = numTrees(i-1);
        int y = numTrees(n-i);
         ans += x*y;
      }
      return ans;
    }
};
